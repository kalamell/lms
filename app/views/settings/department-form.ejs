<div class="container-xxl flex-grow-1 container-p-y">
  <!-- Header -->
  <div class="d-flex align-items-center mb-6">
    <a href="/settings/department" class="btn btn-icon btn-text-secondary rounded-pill me-3">
      <i class="icon-base ri ri-arrow-left-line"></i>
    </a>
    <div>
      <h4 class="mb-1"><%= action === 'create' ? 'Create' : 'Edit' %> Department</h4>
      <p class="text-muted mb-0">
        <% if (action === 'create') { %>
        Add new department
        <% } else { %>
        Edit department: <%= department.name %>
        <% } %>
      </p>
    </div>
  </div>

  <% if (typeof error !== 'undefined' && error) { %>
  <div class="alert alert-danger alert-dismissible mb-4" role="alert">
    <%= error %>
    <button type="button" class="btn-close" data-bs-dismiss="alert"></button>
  </div>
  <% } %>

  <!-- Form -->
  <div class="row">
    <div class="col-md-8">
      <div class="card">
        <div class="card-body">
          <form method="POST" action="/settings/department<%= action === 'edit' ? '/' + department.id + '/edit' : '/create' %>">
            <div class="mb-4">
              <label class="form-label" for="functions_id">Function <span class="text-danger">*</span></label>
              <select class="form-select" id="functions_id" name="functions_id" required>
                <option value="">Select Function</option>
                <% functions.forEach(f => { %>
                <option value="<%= f.id %>" <%= (department && department.functions_id == f.id) ? 'selected' : '' %>>
                  <%= f.format_name %> - <%= f.name %>
                </option>
                <% }) %>
              </select>
            </div>

            <div class="mb-4">
              <label class="form-label" for="name">Department Name <span class="text-danger">*</span></label>
              <input type="text" class="form-control" id="name" name="name"
                     value="<%= department ? department.name : '' %>" required placeholder="Enter department name">
            </div>

            <div class="mb-4">
              <label class="form-label" for="order">Display Order</label>
              <input type="number" class="form-control" id="order" name="order"
                     value="<%= department ? department.order : 999 %>" min="0" placeholder="999">
            </div>

            <div class="mb-4">
              <label class="form-label d-block">Status</label>
              <div class="form-check form-switch">
                <input class="form-check-input" type="checkbox" id="status" name="status"
                       <%= (!department || department.status === 1) ? 'checked' : '' %>>
                <label class="form-check-label" for="status">Active</label>
              </div>
            </div>

            <div class="d-flex gap-2">
              <button type="submit" class="btn btn-primary">
                <i class="icon-base ri ri-save-line me-2"></i>
                <%= action === 'create' ? 'Create' : 'Save Changes' %>
              </button>
              <a href="/settings/department" class="btn btn-secondary">Cancel</a>
            </div>
          </form>
        </div>
      </div>
    </div>

    <% if (action === 'edit' && department) { %>
    <div class="col-md-4">
      <div class="card">
        <div class="card-body">
          <h6 class="mb-3">Information</h6>
          <div class="mb-2">
            <small class="text-muted">ID</small>
            <p class="mb-0"><%= department.id %></p>
          </div>
          <div class="mb-2">
            <small class="text-muted">Created</small>
            <p class="mb-0">
              <% if (department.created_at) { %>
              <%= new Date(department.created_at).toLocaleString('th-TH') %>
              <% } else { %>
              -
              <% } %>
            </p>
          </div>
          <div class="mb-0">
            <small class="text-muted">Updated</small>
            <p class="mb-0">
              <% if (department.updated_at) { %>
              <%= new Date(department.updated_at).toLocaleString('th-TH') %>
              <% } else { %>
              -
              <% } %>
            </p>
          </div>
        </div>
      </div>
    </div>
    <% } %>
  </div>
</div>
